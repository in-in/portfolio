mixin portfolio(list)
  +b.portfolio&attributes(attributes)
    each item in list
      +e('a')(href="#{item.link}").item
        +e('picture').image
          source(
            media="(max-width: 20rem)",
            srcset="#{item.xs}"
            )
          source(
            media="(min-width: 20rem) and (max-width: 30rem)",
            srcset="#{item.s} 1x, #{item.l} 2x"
            )
          source(
            media="(min-width: 30rem) and (max-width: 40rem)",
            srcset="#{item.m}"
            )
          source(
            media="(min-width: 40rem) and (max-width: 60rem)",
            srcset="#{item.m}"
            )
          source(
            media="(min-width: 60rem) and (max-width: 80rem)",
            srcset="#{item.l}"
            )
          source(
            media="(min-width: 80rem)",
            srcset="#{item.l}"
            )
          img(srcset="#{item.xs}", alt="item.title")
        +e.description
          h3= item.title
          p= item.desc
